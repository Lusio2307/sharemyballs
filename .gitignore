/target
*.dll
/config.toml
**/.DS_Store
third_party
*.exe
*.dmg
.idea/
.tmp

# Downloaded artifacts (FFmpeg shared build, etc.)
/.cache/

# Vendored patched crates *are* committed (see [patch.crates-io] in Cargo.toml);
# only their build output is ignored.
/vendor/**/target/

# Deployment scratch: npm cache and upstream clones used for reference only.
/deploy/.npm-cache/
/deploy/external/